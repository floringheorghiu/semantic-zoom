---
description: Embed a Semantic Zoom payload into a markdown file via the embed-zoom-payload skill
agent: build
---

Load and follow the `embed-zoom-payload` skill against this target file: $ARGUMENTS

If no file was given, ask which file before doing anything else — do not guess between candidates.
